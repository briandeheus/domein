# Copy to /etc/domein.env and chmod 600 it.
DOMEIN_PASSWORD=change-me

# Upstream resolvers dnsmasq forwards to.
#DOMEIN_UPSTREAMS=1.1.1.1,9.9.9.9

# DNS port (53) and listen address (empty = all interfaces).
#DOMEIN_DNS_PORT=53
#DOMEIN_DNS_LISTEN=192.168.1.10
