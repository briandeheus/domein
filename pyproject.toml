[project]
name = "domein"
version = "0.1.0"
description = "Self-hosted DNS blocklist manager and local records, with dnsmasq under the hood."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27",
    "itsdangerous>=2.2",
    "python-dotenv>=1.2.2",
]
